BIENVENIDO {
sean
 entero a;
 real b;
 real c;
 entero d[10];
 booleano quiza;
.


a = +10;
b = 10.1;
c = ( b - a ) * 50 / (-(+2));
captar d[0];
quiza = falso;


si ( d > c )
	imprimir "Hola, mundo.";
si_no si ( quiza ){
        sean.
	c = 0;
	imprimir c + 1;
} si_no {
  sean.
  imprimir d[9], "";
}

si ( ! quiza && d[0] == 'c' )
  imprimir "Cadena sin cerrar";



        imprimir 'c', ", _holi", "3", óñn, π, que_no?;
}
