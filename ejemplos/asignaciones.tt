BIENVENIDO {
           sean entero a, b;
                booleano c, d;
                real x, y, z.

           b = a + 3;
           b = a + 3 + 4 * b + 5 /6;
           b = -a + 3;
           
           c = !(!c || d) && (b == a + 3) || b != a;
           d = b < a || a > b || a >= b && a <= b;


           x = (y + z - 3.0) / 4.0;

}
